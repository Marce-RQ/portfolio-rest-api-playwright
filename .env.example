# =============================================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# 
# IMPORTANT: This is a DEVELOPMENT/DEMO project only.
# - These are NOT production-ready secrets
# - Database passwords are intentionally simple for local development
# - JWT_SECRET is a placeholder for development convenience
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update values as needed for your local environment (Completely OPTIONAL)
# 3. The defaults values work out-of-the-box for local development.
#    You dont need to change them for the project to run.
# =============================================================================

# Database Configuration
POSTGRES_USER=api_user
POSTGRES_PASSWORD=api_pass
POSTGRES_DB=fintech_api
DATABASE_URL=postgresql://api_user:api_pass@db:5432/fintech_api

# API Configuration
PORT=3000
NODE_ENV=development
JWT_SECRET=your-secret-key-change-in-production

# Testing Configuration
API_BASE_URL=http://localhost:3000
